Repository for testing Xapain Dynamic Snippets results.

Contents:
    cswiki - a set of Wikipedia plain text parsed documents with cs content.

Testing:
    For indexing Xapian's omindex is used.
    For differend results, the indexed sample size can be altered by using the
    -E parameter

    Snippet results can be viewed using simplesnippet test

    For compiling set INCLUDE_PATH to $XAPIAN_REPO/xapian/xapian-core/include
                      LIBRARY_PATH to $XAPIAN_REPO/xapian/xapian-core/.libs
    
    For running set LD_LIBRARY_PATH to include libxapian-1.3.so
